# Decision Log — Blitzy Sandbox Backstage Refactor

This document fulfills **Rule R3 (Explainability)** of the Agent Action Plan. Every non-trivial decision made during this refactor is captured below with rationale and risks. A decision is non-trivial if a competent engineer could reasonably have chosen differently. Deviations from a literal or obvious interpretation of the requirements appear here as explicit entries; unexplained deviations are treated as defects.

This file is **distinct** from the upstream Backstage ADRs in [`../architecture-decisions/`](../architecture-decisions/). The ADR format (numbered files with id / title / description front matter and Context / Decision / Consequences sections) is reserved for upstream governance records. This refactor decision log is a single flat Markdown table specific to this pull request.

For the requirement → file/test mapping, see [`traceability-matrix.md`](traceability-matrix.md). For the visual before/after diagrams, see [`architecture-before-after.md`](architecture-before-after.md). For onboarding guidance, see [`onboarding-addendum.md`](onboarding-addendum.md). For follow-on improvements, see [`next-tasks.md`](next-tasks.md).

---

## 0. How to Read This Document

Each decision below is a four-column row. Columns mean exactly:

- **Decision** — A single, concrete choice and the artifact it produces. Reads as "what we did".
- **Alternatives Considered** — Every option that was on the table at the moment of decision. Reads as "what we did not do, and why the choice was not obvious".
- **Rationale** — The principled reason the chosen alternative was selected. Links back to AAP sub-sections where applicable.
- **Risks** — The downside the decision carries and the mitigation that reduces residual risk to acceptable.

Reviewers cross-referencing the code should find a pointer to this decision log, not inline rationale comments. AAP §0.7.1.3 (R3 rule) states: _"Do not embed rationale in code comments. The decision log is the single source of truth for 'why' decisions."_

The decisions in Section 1 are sequenced from most-user-visible (catalog count, top-bar, audit middleware) to most-internal (policy packaging, logo mechanism, email source). This sequencing helps reviewers triage the highest-impact decisions first. Section 2 documents decisions that deviate from a literal interpretation of the user's requirements and therefore require explicit explanation per R3.

---

## 1. Decisions

The table below has four columns: `Decision`, `Alternatives Considered`, `Rationale`, and `Risks`. The six rows in this section are mandatory per the folder-level specification of this refactor; additional rows appear in Section 2.

Each row corresponds to one of the four workstreams enumerated in AAP §0.5.1: Workstream A (Chrome Refactor) → row 2; Workstream B (Catalog UI Surgery) → row 5; Workstream C (Authorization, Audit, User Tracking) → rows 3, 4, 6; Workstream D (Dashboard Removal, Routing, Catalog Count Fix) → row 1. The unit test coverage requirement (AAP §0.7.2: >80% for new/modified authentication/authorization logic) is the implicit driver behind row 4's packaging choice.

| #   | Decision                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | Alternatives Considered                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | Rationale                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | Risks                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| --- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | Catalog count fix strategy: keep `EntityTagFilter.getCatalogFilters()` emitting the wire-format-compatible OR-shape `{ 'metadata.tags': this.values }` and correct the user-visible count in the React layer. AND-narrowing of rows is enforced by `EntityTagFilter.filterEntity` (`Array.prototype.every`). AND-narrowing of the displayed total under multi-tag paginated modes is enforced by `useEntityListProvider.tsx`, which issues a secondary unpaginated `catalogApi.getEntities()` request and computes `totalItems = full.items.filter(entityFilter).length` only when more than one tag is selected. | (a) Backend filter rewrite: change `getCatalogFilters()` to emit per-tag clauses (e.g. `{ 'metadata.tags.0': 'java', 'metadata.tags.1': 'spring' }` or a compound shape) that the backend's `EntitiesSearchFilter` evaluates as AND, making `response.totalItems` correct without any React-layer recount. (b) Pure single-page frontend recount: derive `totalItems = response.items.filter(entityFilter).length` from the paginated page without issuing a secondary `getEntities()` call. (c) SQL-level fix in `plugins/catalog-backend/src/database/`: rewrite the catalog query to apply AND semantics on `metadata.tags`. | The wire format `EntityFilterQuery` (`Record<string, string \| symbol \| (string \| symbol)[]>`) has no path to emit AND across same-key values: the catalog backend's filter parser dedups same-key values into a single `EntitiesSearchFilter` that is evaluated as OR. Alternative (a) was therefore rejected as not wire-compatible. Alternative (b) produces an undercount whenever the AND-narrowed result spans more pages than the current view (page size = 20 with 12 of 50 AND-matches scattered across pages produces partial counts). Alternative (c) is out of scope per AAP §0.3.2 ("Catalog backend persistence layer"). The chosen two-layer design uses the existing AND predicate (`filterEntity.every()`) for rendered rows and applies the same predicate to an unpaginated `getEntities()` result for the total, satisfying the user's diagnosis (AAP §0.1.3) — "the actual catalog items displayed are correct; only the count displayed at the top is wrong" — without changing the wire format or the backend. Implementation: `plugins/catalog-react/src/filters.ts` JSDoc on `EntityTagFilter` (L73-L107), and the `computePaginatedTotalItems` helper in `plugins/catalog-react/src/hooks/useEntityListProvider.tsx` (L277-L320). | The secondary unpaginated `getEntities()` call adds one extra HTTP round-trip per filter change while multi-tag selection is active (single-tag selection still uses the paginated `queryEntities` total directly). Mitigation: the secondary call only fires when `tagsFilter.values.length > 1`, so single-tag and tag-cleared interactions remain on the fast path. If the secondary call fails, the hook falls back to `response.totalItems` (the OR-superset) so the UI is not blocked — this fallback is documented in the `EntityTagFilter` JSDoc and in `architecture-before-after.md` §3.2. Regression coverage lives in `plugins/catalog-react/src/hooks/useEntityListProvider.test.tsx` and `plugins/catalog-react/src/filters.test.ts`. |
| 2   | Top-bar implementation pattern: create a new frontend module `packages/app/src/modules/appModuleTopBar.tsx` that mounts Logo / Settings / Support into `Header.rightItemsBox` via `NavContentBlueprint.make({ params: { component } })` plus an `appPlugin.getExtension('app/layout').override(...)` factory that replaces the upstream layout's nav slot with the new top-bar component.                                                                                                                                                                                                                         | (a) Monkey-patch `packages/core-components/src/layout/Header/Header.tsx` to hardcode the cluster. (b) Fork `@backstage/core-components` to add the top-bar as a native feature. (c) Use a `HeaderLayoutBlueprint`-style extension.                                                                                                                                                                                                                                                                                                                                                                                              | Preserves upstream upgrade compatibility because no upstream file is modified. Uses Backstage's published frontend module composition extension points, which are the canonical pattern for new chrome modules (AAP §0.5.6 "Design pattern — Modular frontend composition"). The chosen pair (`NavContentBlueprint` + `app/layout` override) is the same pattern the deleted `appModuleNav.tsx` used for the sidebar, so the diff is mirror-image, reviewable, and rollback is a one-line revert in the `App.tsx` features array. Alternative (c) is rejected because `@backstage/frontend-plugin-api` at the workspace pin does not export a `HeaderLayoutBlueprint` symbol — the `NavContentBlueprint` + `app/layout` override pair is the actual supported pattern in Backstage 1.48.0.                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | The `NavContentBlueprint` API plus the `app/layout` override surface are part of the newer Backstage frontend system and may evolve. If either changes shape in a future upstream minor release, the module file must be adapted; the change is isolated to a single file (`appModuleTopBar.tsx`).                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| 3   | Audit middleware location: create a new internal catalog backend module `plugins/catalog-backend-module-access-audit/` that wraps entity reads through an Express middleware mounted on the catalog plugin's router and emits `entity-access` audit events via `coreServices.auditor`.                                                                                                                                                                                                                                                                                                                            | (a) Backend-wide HTTP interceptor in `packages/backend/src/index.ts` that fires for every `/api/catalog/...` request. (b) Inline catalog service wrapper at the composition root (one-off wrapper around `catalogServiceRef` in `packages/backend/src/index.ts`).                                                                                                                                                                                                                                                                                                                                                               | Isolated and testable: the new module lives behind its own package boundary and hosts its own Jest test suite at `plugins/catalog-backend-module-access-audit/src/module.test.ts`, which covers by-name and by-uid success paths, non-GET exclusion, collection and ancestry exclusion, success and fail finalization, principal variations, auditor failure degradation, finish+close idempotency, canonicalization, and response-body fallback behavior across 25 executed cases. Follows the Backstage backend module pattern (`createBackendModule({ pluginId: 'catalog', moduleId: 'access-audit', ... })`) — the same shape as upstream extension modules. The HTTP-interceptor alternative would have audited unrelated catalog endpoints (search, refresh, registration) which would inflate the `entity-access` event volume and dilute the signal. Because the middleware has access to the Express `Request` object, the emitted audit events DO carry `request: req` (in contrast to the sign-in resolver, which lacks `ctx.request` and must synthesize a `correlationId` — see Row 6 below).                                                                                                                                                    | A subset of read paths could bypass the middleware if some plugin reads catalog entities through a private API not covered by the wrap. Mitigation: the middleware regex-matches the canonical `GET /entities/by-name/...` and `/entities/by-uid/...` paths; any newly-introduced read path requires a tandem test addition. The committed `module.test.ts` suite is the regression surface that detects such gaps.                                                                                                                                                                                                                                                                                                                                 |
| 4   | Permission policy plugin packaging: create a new internal plugin `plugins/permission-backend-module-blitzy-policy/` containing the `BlitzyPermissionPolicy` class, its `createBackendModule` registration, and its unit tests.                                                                                                                                                                                                                                                                                                                                                                                    | (a) Inline `BlitzyPermissionPolicy` class in `packages/backend/src/policy.ts` next to `index.ts`. (b) Mix the policy into the existing `plugins/permission-backend-module-policy-allow-all/` package (rename + modify in place).                                                                                                                                                                                                                                                                                                                                                                                                | Testable in isolation: the >80% unit-coverage requirement on the policy module (AAP §0.7.2) is easier to enforce when the policy lives in its own Jest test surface with its own coverage report. Follows the precedent of `plugins/permission-backend-module-policy-allow-all/` — both packages have the same shape (`src/policy.ts`, `src/module.ts`, `src/index.ts`, `package.json`, `tsconfig.json`, `README.md`, `catalog-info.yaml`, `.eslintrc.js`). The PR's `backend.add(import('@internal/plugin-permission-backend-module-blitzy-policy'))` line in `packages/backend/src/index.ts` is symmetric with the deleted allow-all registration.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | One additional plugin to maintain (one more `package.json`, one more `tsconfig.json`, one more `catalog-info.yaml`). Mitigation: copy-from-template using the allow-all package structure verbatim minimizes maintenance overhead; the new plugin is small (approximately 100 lines of TypeScript).                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| 5   | Logo non-clickability mechanism: render the Blitzy SVG as a plain inline element inside a `<div>` with NO `<Link>` wrapper and NO click handler.                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | (a) Wrap in `<Link to="#" onClick={e => e.preventDefault()}>` to keep the `<a>` semantics for keyboard focus. (b) Apply `pointer-events: none` CSS to the existing `<Link>` wrapper. (c) Conditionally render the `<Link>` based on a prop.                                                                                                                                                                                                                                                                                                                                                                                     | Semantically inert: no `<a>` element means no keyboard-focus regression, no incorrect ARIA role, and no screen-reader-announced "link" affordance for an element that is not navigable. Matches the user's verbatim requirement "remove the ability to click on the logo" (AAP §0.1, §0.5.4). The `<Link to="#">` alternative leaves a clickable region that requires JS to suppress, which can still be activated under edge cases (for example, middle-click opens a blank tab on `#`).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | Any future requirement to make the logo clickable again will require re-wrapping with `<Link>` and re-evaluating the navigation target. Mitigation: the change is one-file and one-line — easily reverted.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| 6   | Email source priority for the policy's domain check: read `result.fullProfile.emails[?].primary` first, fall back to `result.userinfo.email`, then to a synthetic `<username>@unknown.invalid` so the policy can apply a deterministic non-Blitzy decision.                                                                                                                                                                                                                                                                                                                                                       | (a) Rely solely on `result.userinfo.email` (the OIDC userinfo payload). (b) Query GitHub `/user/emails` via the access token on every sign-in.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | Matches Backstage convention: `result.fullProfile` is the GitHub-rich profile available to the resolver and is the canonical source of identity data. No additional outbound HTTP call to the GitHub API on every sign-in (which would inflate latency and hit rate limits). The fallback chain ensures the policy always has a domain string to evaluate, so read-only enforcement remains deterministic even when email is private or absent. The synthetic `@unknown.invalid` domain is RFC 2606 reserved and cannot match `@blitzy.com`, so it cannot accidentally elevate access.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | GitHub may not expose an email at all if the user has set it to private and the OAuth scope does not request `user:email`. Mitigation: the fallback to `<username>@unknown.invalid` treats this as a non-Blitzy principal and constrains them to read-only — the safe-by-default posture. Documented in [`onboarding-addendum.md`](onboarding-addendum.md) §6 (Policy Customization) and in [`../auth/identity-resolver.md`](../auth/identity-resolver.md).                                                                                                                                                                                                                                                                                         |

### 1.1 Notes on Section 1

- Row 1 is the bug fix the user explicitly diagnosed. Its rationale draws directly from the user's quoted observation: "the actual catalog items displayed are correct; only the count displayed at the top is wrong." (AAP §0.1.3)
- Row 2 sets the chrome substrate for every other UI/UX change. Without a layout-blueprint mount point, Settings / Support / Logo have nowhere to go after the sidebar is removed.
- Rows 3, 4, and 6 together implement AAP §0.5.1.3 (Workstream C — Authorization, Audit, and User Tracking). They are co-dependent: the policy (row 4) reads the email extracted by the resolver (row 6); the audit middleware (row 3) emits events that prove the policy decisions are tracked.
- Row 5 is the smallest decision but disproportionately consequential for accessibility — the alternative "Link with preventDefault" is a footgun that ships in many sites and degrades the experience for keyboard and screen-reader users.

---

## 2. Additional Decisions

The following additional decisions were made during implementation and are tracked here for completeness. The six rows in Section 1 are mandatory; the rows in this section document deviations from a literal interpretation of the requirements (AAP §0.7.1.3 R3: _"Any deviation from a literal or obvious interpretation of the requirements MUST have an explicit entry in the decision log"_).

These three rows together cover the three deviations the AAP itself flagged as implicit prerequisites the user did not state explicitly (AAP §0.1.1).

| #   | Decision                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | Alternatives Considered                                                                                                                                                                                                                                                                                                                                                                                                                                                 | Rationale                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | Risks                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| --- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 7   | Feature flag posture: no feature flags are introduced. The transition from sidebar to top-bar, from allow-all to `BlitzyPermissionPolicy`, and from OR-count to AND-count is a hard cut at deploy time.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | (a) Add `app.featureFlags.topBarChrome` and `app.featureFlags.blitzyPolicy` to allow gradual rollout. (b) Stage the changes across multiple PRs with intermediate feature-flagged states.                                                                                                                                                                                                                                                                               | Single-PR delivery aligns with the user's explicit directive "Deliver a complete, pull-request-ready solution." (AAP §0.7.2). Feature flags would introduce a parallel-state burden — the codebase would need to keep both the sidebar and the top-bar working until the flag flips — adding complexity disproportionate to the simplification this refactor delivers.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | No in-flight rollback for individual surfaces — if the top-bar misrenders in production, the only rollback is a full revert of the PR. Mitigation: the Playwright E2E suite must pass on all three browsers (chromium, firefox, webkit) before merge; the visual regression baselines under `packages/app/e2e-tests/__screenshots__/` catch unintended visual changes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| 8   | Catalog data-model preservation: `System` and `Owner` relations remain present in entity YAMLs and in the catalog database. Only the UI surfaces that display them are removed.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | (a) Remove `System` and `Owner` relations from `@backstage/catalog-model` entirely and run a Knex migration to drop the associated tables/columns. (b) Filter `System` and `Owner` relations out at the catalog API layer so consumers never see them.                                                                                                                                                                                                                  | The user requested "Perform a full removal of this functionality across the application" (AAP §0.1.3). Interpretation: the user surface (UI) is the affected scope, not the data layer. Removing the data layer would (i) break out-of-band consumers (catalog API, GraphQL extensions, automation scripts) that may still rely on the relations, (ii) require an irreversible Knex migration that could not be rolled back without data loss, and (iii) exceed the AAP §0.3.2 out-of-scope boundary "Catalog backend persistence layer".                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | Future contributors who see Owner/System data still flowing into entities may believe the removal is incomplete. Mitigation: this row and the bullet in [`onboarding-addendum.md`](onboarding-addendum.md) §8 (Common Pitfalls) document the boundary explicitly.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| 9   | Per-entity TechDocs preservation: the global `/docs` index page is removed but the `EntityTechdocsContent` per-entity extension stays so TechDocs are accessible after a user clicks into a project entity.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | (a) Remove all TechDocs extensions, including per-entity Documentation tabs. (b) Replace TechDocs with a first-party in-repo doc renderer.                                                                                                                                                                                                                                                                                                                              | The user requested "Remove the 'Documentation' tab" (AAP §0.1.3) — the explicit referent is the global Documentation tab in primary navigation, not the per-entity Documentation tab on the project entity page. The implicit prerequisite "TechDocs tab preserved on entity pages" is called out in AAP §0.1.1. Preserving the per-entity tab maintains a navigation path to project-specific docs (which the user did not ask to remove). The replacement alternative is captured as a follow-on in [`next-tasks.md`](next-tasks.md).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | Users may incorrectly believe the per-entity Documentation tab is also gone after reading the requirement summary. Mitigation: [`../getting-started.md`](../getting-started.md) explicitly clarifies that TechDocs are accessible per-entity after the refactor; the assertion in `packages/app/e2e-tests/refactor.test.ts` verifies the global tab is absent without asserting per-entity tab absence.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| 10  | On-behalf-of token gap closure: introduce an in-process email cache (`packages/backend/src/userEmailCache.ts`, a module-scoped `Map<userEntityRef, email>` with FIFO eviction at 10,000 entries) and a custom `BlitzyUserInfoService` (`packages/backend/src/userInfoServiceFactory.ts`) registered via `backend.add(blitzyUserInfoServiceFactory)`. The sign-in resolvers (`authModuleGithubProvider.ts` and `authModuleBlitzyE2E.ts`) call `cacheUserEmail(userEntityRef, email)` AFTER successful token issuance. The custom user-info service reads `email` from the JWT when present, falls back to the cache when absent, and surfaces it on `BackstageUserInfo.email` (via structural cast). `BlitzyPermissionPolicy` reads `user.info.email` FIRST and decodes the JWT only as a fallback. | (a) Modify `plugins/permission-backend/src/service/router.ts` to pass the user's original credentials/token to the policy instead of the on-behalf-of token. (b) Have the policy fetch the email from the catalog every time via a `CatalogService` injected dependency. (c) Persist the email cache to a database (SQLite, Redis, Knex). (d) Re-mint the on-behalf-of token to include the `email` claim by extending Backstage's `AuthService.getPluginRequestToken`. | QA CP5 Critical Defect #2 ("@blitzy.com users DENIED writes through real Backstage endpoints") root-caused the issue to the on-behalf-of token: `auth.getPluginRequestToken({ onBehalfOf, targetPluginId })` mints a plugin-to-plugin token that contains only `sub`, `ent`, `act`, `aud` — the user's `email` claim is stripped. The policy's `extractEmail()` therefore returned `undefined` for ALL internal write paths, classifying every @blitzy.com user as "other" and denying their writes. The chosen fix surfaces `email` through `user.info.email` (the policy's PRIMARY lookup path), bypassing the on-behalf-of token entirely. The cache populates synchronously at sign-in, before any permission check for the new session can occur. Alternative (a) is rejected because it modifies upstream Backstage code, breaking upgrade compatibility. Alternative (b) adds a network round-trip to every permission check, breaking the O(1) stateless policy contract. Alternative (c) adds a database dependency and cache-invalidation complexity disproportionate to the requirement — the in-process cache is sufficient because (i) it is populated at sign-in before any check, (ii) stale entries are harmless (the policy fails closed to DENY when email is missing), and (iii) restart-on-deploy resets the cache safely. Alternative (d) modifies the `AuthService` contract, breaking upstream upgrade compatibility for all consumers of plugin-to-plugin tokens. The implementation has been unit-tested at `packages/backend/src/userEmailCache.test.ts` (FIFO eviction at boundary, re-insertion behavior, validation), `packages/backend/src/userInfoServiceFactory.test.ts` (JWT path, cache fallback, missing token, malformed JWT, write-through), and `packages/backend/src/authModuleGithubProvider.test.ts` + `authModuleBlitzyE2E.test.ts` (cache population on successful sign-in, no-cache on failed sign-in). | The cache is bounded at 10,000 entries; deployments serving more than 10,000 simultaneous authenticated sessions will see cache thrash where evicted users fall through to the JWT-decode fallback (which fails for on-behalf-of tokens), resulting in DENY for legitimate users. Mitigation: the `MAX_CACHE_ENTRIES` constant in `userEmailCache.ts` can be raised at deployment time; memory cost is ~0.5 KB/entry (so 100,000 entries cost ~50 MB). The 10,000-entry default is documented in [`onboarding-addendum.md`](onboarding-addendum.md) §9.6 with explicit guidance on when to raise the constant. A second risk: the cache is process-local, so deployments with multiple backend replicas behind a load balancer have N copies of the cache, each populated only by sessions whose sign-in landed on that replica. A user signed in via replica A but issuing a write through replica B would miss the cache on replica B and fall through to the JWT-decode fallback. Mitigation: the JWT-decode fallback DOES succeed for the user's original JWT (the direct `/api/permission/authorize` path), so single-replica miss is only consequential for the internal on-behalf-of path. For multi-replica deployments where this matters, raise to a persistent cache (entry 13 below documents the migration path). |
| 11  | E2E audit-events endpoint: convert `blitzyE2EAuditEndpointModule` (a `createBackendModule({ pluginId: 'blitzy-e2e', moduleId: 'audit-events-endpoint' })`) into `blitzyE2EAuditEndpointPlugin` (a `createBackendPlugin({ pluginId: 'blitzy-e2e' })`) so it self-registers when added to the backend. Both registrations remain gated by `BLITZY_E2E_TEST_MODE=true`.                                                                                                                                                                                                                                                                                                                                                                                                                               | (a) Keep `createBackendModule` but also register a stub parent plugin with `pluginId: 'blitzy-e2e'` so the module's `pluginId` resolves to an actual host plugin. (b) Change the module's `pluginId` to an existing plugin (`'catalog'` or `'app'`) and adjust the route path to `/api/catalog/blitzy-e2e/audit-events`. (c) Mount the endpoint via a backend-wide HTTP route registration outside the plugin/module system.                                            | QA CP5 Critical Defect #1 (`/api/blitzy-e2e/audit-events` returns 404) root-caused the issue to Backstage's backend system contract: `createBackendModule({ pluginId: X, moduleId: Y })` initializes only when a plugin with `pluginId: X` is registered (per `BackendInitializer.ts` lines 360-378). Because no plugin with `pluginId: 'blitzy-e2e'` was registered anywhere in the backend, the module was silently dropped at init — the routes never mounted. The chosen fix converts the module to a plugin so it self-registers, eliminating the "matching parent plugin" requirement entirely. Alternative (a) requires creating a stub plugin with no real content, doubling the registrations. Alternative (b) misroutes the endpoint under a semantically unrelated plugin's path, making the test-only nature less discoverable. Alternative (c) bypasses the backend system entirely and would lose the dependency-injection benefits (auditor, logger, httpRouter all injected via `deps`). The chosen approach is the minimum-diff fix that follows Backstage's own conventions. A smoke E2E test in `packages/app/e2e-tests/auditing.test.ts` asserts the endpoint is reachable (2xx or 401, NOT 404) when `BLITZY_E2E_TEST_MODE=true`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | Renaming the export changes any external import path. Mitigation: the export was internal to `packages/backend/` and only one caller exists (`packages/backend/src/index.ts`), so the impact is contained. Generated `.d.ts` files in `dist-types/` still reference the old name and will be regenerated on next build. A second risk: the plugin self-registers under `pluginId: 'blitzy-e2e'` which means any other future module that uses `createBackendModule({ pluginId: 'blitzy-e2e', moduleId: X })` will now wire correctly — desirable for future test-mode extensions but worth tracking in case a name collision is later wanted.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| 12  | User-login audit emission for the `blitzy-e2e` provider: convert the resolver from a top-level constant (`const blitzyE2ESignInResolver = ...`) to a factory function (`createBlitzyE2ESignInResolver(auditor)`) that mirrors the GitHub provider's audit lifecycle. The factory wires the audit emission, the `cacheUserEmail` write, and the `user_login_total` counter increment around `ctx.issueToken`.                                                                                                                                                                                                                                                                                                                                                                                       | (a) Leave the blitzy-e2e resolver audit-emission-free and rely solely on the GitHub provider for `user-login` events. (b) Emit `user-login` events from a separate post-sign-in event listener wired into the backend root. (c) Emit synthetic `user-login` events from the audit-events HTTP endpoint when the test fixture pings it.                                                                                                                                  | QA CP5 Major Issue #3 documented that the `user-login` audit emission contract had zero runtime test coverage because the GitHub provider is the only emitter and GitHub OAuth is not configured in the test environment. Without runtime test coverage, the AAP §0.7.2 mandate ">80% unit coverage for new/modified Authentication/Authorization logic" is satisfied by the GitHub provider's unit tests but not exercised end-to-end. The chosen fix extends the blitzy-e2e provider (used by E2E tests) to emit `user-login` events with the same shape (`severityLevel: 'medium'`, `meta: { provider, username, emailDomain, userEntityRef, correlationId }`), so Playwright fixtures can sign in via the test provider and assert the event in the audit channel. Alternative (a) leaves the gap. Alternative (b) adds a parallel emission path that drifts from the provider's lifecycle (the original auth resolver could fail and still trigger the event listener). Alternative (c) couples the audit emission contract to the test fixture, making the contract invisible to non-test code paths. The chosen approach mirrors the GitHub provider's lifecycle exactly: emit `auditor.createEvent({...})` BEFORE `ctx.issueToken`, then `.success({...})` on success or `.fail({...})` on rethrow. The cache write happens between `issueToken` and `.success` so a failed token issuance does not pollute the cache.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | The factory pattern adds one level of indirection vs. the top-level constant. Mitigation: the factory is invoked exactly once in the backend module registration with `createBlitzyE2ESignInResolver(auditor)`, so the indirection is local. The unit tests in `packages/backend/src/authModuleBlitzyE2E.test.ts` exercise the full lifecycle: success path, failure path, audit event meta, cache population, counter emission, PII discipline (no full email in meta).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| 13  | Custom UserInfoService registration in `packages/backend/src/index.ts` via `backend.add(blitzyUserInfoServiceFactory)`. The factory is registered BEFORE any plugin imports so it takes precedence over the default `userInfoServiceFactory` registered by `backend-defaults`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | (a) Register the custom factory inside the permission backend module instead of at the backend root. (b) Modify the default `userInfoServiceFactory` from `@backstage/backend-defaults` in place (forking the package). (c) Wrap each call site that consumes `UserInfoService` with a custom decoder.                                                                                                                                                                  | The Backstage `ServiceRegistry` (`packages/backend-app-api/src/wiring/ServiceRegistry.ts`) seeds default factories via `factoryMap.set()` at construction (lines 60-76) WITHOUT tracking them in `#addedFactoryIds`. The `add()` method (lines 201-232) enforces uniqueness via `#addedFactoryIds.has()`, so a custom factory added via `backend.add(...)` CAN override a default factory that was seeded but not added — the default is silently replaced. `coreServices.userInfo` is one of the services that allows override (in contrast to `coreServices.pluginMetadata`, which throws at line 203-206 when re-added). Registering at the backend root means EVERY plugin (catalog, permission, auth, etc.) sees the same `UserInfoService` instance — required because the policy and the audit middleware both consume it. Alternative (a) registers the factory in a plugin module, but the `ServiceRegistry` is per-plugin in the new backend system, so the override would only take effect for that one plugin's consumers — defeating the purpose. Alternative (b) forks `@backstage/backend-defaults`, which violates AAP §0.5.6 ("Design pattern — Modular frontend composition" and equivalent backend composition). Alternative (c) requires patching every call site, expanding the diff surface and creating drift hazards. The chosen approach is the minimum-diff fix that uses Backstage's published service-override extension point.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | The override is silent — there is no startup log line announcing "custom UserInfoService active". Mitigation: a one-line `logger.info('BlitzyUserInfoService active')` could be added inside the factory's `factory({discovery})` callback if operators need visual confirmation. The choice of "no log line" matches the silence of the default factory; both are quiet by convention. A second risk: if a future Backstage release changes the override semantics so that `add()` rejects an `add()` on a service whose default was already set, the registration will fail at startup. Mitigation: the unit test at `packages/backend/src/userInfoServiceFactory.test.ts` exercises the factory's `getUserInfo()` behavior; if registration ever fails at startup, the smoke E2E in `packages/app/e2e-tests/auditing.test.ts` (which performs a sign-in and asserts the audit event) will fail visibly.                                                                                                                                                                                                                                                                                                                                                                                                                     |

| 14 | Auth-backend OIDC CVE remediation (CVE-2026-32235 / CVE-2026-32236): remove the `auth.experimentalDynamicClientRegistration` and `auth.experimentalClientIdMetadataDocuments` blocks from `app-config.yaml` entirely so both features fall back to disabled in `OidcService.getConfiguration()` (`getOptionalBoolean(...)` returns `undefined`, which the conditional branch treats as `false`). The standard OIDC server endpoints (`/.well-known/openid-configuration`, `/v1/token`, `/v1/userinfo`, `/.well-known/jwks.json`) continue to function. A short YAML comment retains a pointer to this row for the next operator. | (a) Upgrade `@backstage/plugin-auth-backend` from `0.27.0` to `>= 0.27.1` to consume the upstream patches `6042dd0c7f0706e0f473dafa92799ecf19c825ec` (DCR allowlist bypass fix) and `17038abf2dfdb4abc08a59b1c95af39851de0e07` (CIMD redirect SSRF fix). (b) Tighten `allowedRedirectUriPatterns` to drop the `http://localhost:*` / `http://127.0.0.1:*` wildcard-port forms in favor of explicit ports while keeping the experimental features enabled. (c) Keep the features enabled but firewall the `/oidc/register` and `/oidc/client-id-metadata` routes at the reverse proxy layer. | The two experimental features are not consumed by any active plugin in this fork: `mcp-actions-backend` (the canonical DCR/CIMD consumer per `plugins/mcp-actions-backend/README.md`) is NOT registered in `packages/backend/src/index.ts`, and no `cursor://` redirect-URI consumer exists anywhere in the repo. Disabling them therefore has zero runtime impact on the deployed feature set while removing the precondition that both GHSA advisories require (`enabled: true`). Alternative (a) is the upstream-canonical fix, but the auth-backend in this monorepo is workspace-vendored at `plugins/auth-backend/` and a true version bump would require either (i) re-vendoring the patched code from upstream 0.27.1, which is a multi-file change with its own regression surface and out of scope per AAP §0.3.2 ("All upstream Backstage plugins not enumerated above"), or (ii) cherry-picking the two specific fix commits into the local fork, which couples this PR's risk surface to subsequent merge conflicts. Alternative (b) reduces but does not eliminate the bypass — the advisory's analysis is that the matcher itself, not the patterns, is unsafe under specific redirect URI shapes. Alternative (c) shifts the security boundary outside the application and is fragile against misconfigured reverse proxies. The chosen approach is the QA report's Option B verbatim. | Re-enabling DCR or CIMD in the future without first upgrading the auth-backend recreates the CVE exposure. Mitigation: the YAML comment block above the `### Add auth.keyStore.provider` line in `app-config.yaml` carries an inline reminder to upgrade auth-backend to `>= 0.27.1` before re-enabling, and the audit verification in `packages/app/e2e-tests/auditing.test.ts` indirectly catches a regression because the affected auth-backend code path is exercised by sign-in flows. A secondary risk: any future deployment that legitimately needs OIDC dynamic client registration (for example, a real Cursor IDE integration) will require the upgrade as a prerequisite, adding work to that future PR. Mitigation: documented in `docs/refactor/next-tasks.md` as the upgrade path. |
| 15 | `/metrics` endpoint hardening: bind the `PrometheusExporter` in `packages/backend/src/instrumentation.js` to `127.0.0.1` by default (configurable via the `PROMETHEUS_BIND_HOST` environment variable for deployments that need a different bind interface, e.g., a private network IP in Kubernetes). The port stays fixed at `9464`. The `target_info` metric, which carries `host_id`, `process_owner`, `process_command`, `process_executable_path`, `process_runtime_version`, and `process_command_args` labels via OpenTelemetry's auto-instrumentation, becomes unreachable from external network neighbors after the bind change. | (a) Add HTTP basic auth or token auth on the `/metrics` endpoint by mounting a custom HTTP server wrapping the `PrometheusExporter`. (b) Set the exporter option `withoutTargetInfo: true` (and `withoutScopeInfo: true`) to suppress the `target_info` metric so that the disclosure surface shrinks even while the port stays bound to `0.0.0.0`. (c) Leave the bind at `0.0.0.0:9464` and rely on a Kubernetes NetworkPolicy / firewall rule / cloud security group to restrict access at the network layer. (d) Bind to a Unix domain socket instead of TCP so only co-located processes can scrape. | The `PrometheusExporter` from `@opentelemetry/exporter-prometheus` exposes a first-class `host` option in `ExporterConfig` (verified in `node_modules/@opentelemetry/exporter-prometheus/build/src/export/types.d.ts`); the default is "all interfaces" because that is the safest assumption for a wide range of integration scenarios. For a single-process backend behind a reverse proxy, `127.0.0.1` is strictly safer and matches the QA report's "Suggested Fix" verbatim. The env-var override (`PROMETHEUS_BIND_HOST`) keeps the door open for legitimate cross-host scraping (a Kubernetes sidecar or a host-network Prometheus on a different node) without requiring a code change — operators can opt into `0.0.0.0` if their network layer enforces the boundary. Alternative (a) adds an auth-handler surface that itself becomes attacker-reachable and complicates scraper configuration (the Prometheus server would need bearer-token support); the Backstage auth-backend's identity tokens are not designed for machine-to-machine scraper use. Alternative (b) reduces disclosure but does not address the fundamental "anyone on the network can read aggregate request volumes" concern. Alternative (c) moves the security boundary outside the application and depends on deployment-environment hygiene — fragile in development environments where firewalls are absent. Alternative (d) breaks compatibility with all common Prometheus scraper configurations, which assume TCP. | Operators who previously scraped `:9464` from a different host without configuring `PROMETHEUS_BIND_HOST` will see their scraper return connection-refused after deploying this change. Mitigation: documented in `docs/observability/dashboards.md` §4.1 and `docs/refactor/onboarding-addendum.md` §9.3 (both updated alongside this code change); `packages/backend/prometheus.yml` continues to target `host.docker.internal:9464`, which works correctly for Docker-Desktop scraping configurations where `host.docker.internal` resolves to the host's loopback. For host-network or remote-network Prometheus, the operator sets `PROMETHEUS_BIND_HOST=0.0.0.0` (or a specific private IP) on the backend process. A second risk: integration tests that scrape the endpoint from a non-local context would also need the env var. Mitigation: the existing E2E tests scrape only from `localhost`, so no test update is required. |
| 16 | LocalGCP Docker Compose container source: build LocalGCP from the upstream public tarball asset via a new `Dockerfile.localgcp` at the repository root. The `services.localgcp` entry in `docker-compose.localgcp.yml` replaces the prior unreachable `image: ghcr.io/slokam-ai/localgcp:v0.6.0` with a `build:` block (`context: .`, `dockerfile: Dockerfile.localgcp`, `args: LOCALGCP_VERSION: '0.6.0'`) paired with a local `image: blitzy-sandbox-localgcp:v0.6.0` tag. The Dockerfile is a two-stage `debian:bookworm-slim` build: stage 1 downloads `localgcp_<version>_linux_<TARGETARCH>.tar.gz` from `https://github.com/slokam-ai/localgcp/releases/download/v<version>/...`, verifies the binary by invoking `./localgcp --help`, and stages the extracted binary; stage 2 installs the binary at `/usr/local/bin/localgcp`, creates `/data/.localgcp` with `chmod 0777`, EXPOSEs `4443 8085 8086 8088 8089 8090 8091 8092 8093`, and sets `ENTRYPOINT ["/usr/local/bin/localgcp"]` plus `CMD ["up", "--data-dir=/data/.localgcp", "--no-docker"]`. | (a) Stay with `image: ghcr.io/slokam-ai/localgcp:v0.6.0` and document the GHCR-credential requirement for developers and CI runners. (b) Republish the LocalGCP image to a Blitzy-controlled public registry (Docker Hub or a public Blitzy GHCR namespace under our own organization). (c) Downgrade `docker-compose.localgcp.yml` to "advanced/CI-only" status, mark the compose path as unsupported, and require the host-binary install (§2.1 of [`onboarding-addendum.md`](onboarding-addendum.md)) as the canonical workflow for every developer and CI step. (d) Build LocalGCP from its open-source Go sources (`go build`) instead of the released tarball, treating the canonical release as untrusted. | QA Checkpoint 13 Critical Issue #1 documented that `ghcr.io/slokam-ai/localgcp` returns `unauthorized` from anonymous pulls across every tag attempted (`:v0.6.0`, `:latest`, `:main`, `:0.6.0`, `:v0.6`, `:v0`, `:stable`) — the image namespace is not anonymously published. The compose path that AAP §0.4.3 promises ("a `docker-compose.localgcp.yml` is created so that CI and developer machines without the host-installed binary can `docker compose -f docker-compose.localgcp.yml up -d` to provision the emulators") was therefore aspirational, not functional. Alternative (a) leaves Rule R6 ("no test, local dev workflow, or CI step may require real GCP credentials") only partially satisfied: the host-binary fallback works but the compose-based path that AAP §0.4.3 prescribes is unreachable, and any developer who follows the documented Option B path hits an authentication error. Alternative (b) introduces a publish-and-maintain burden (image versioning, vulnerability rescans, GHCR / Docker Hub namespace governance, automated rebuild on upstream release) that is disproportionate to a single internal emulator and is outside the AAP's enumerated workstreams. Alternative (c) contradicts the AAP §0.4.3 commitment and downgrades the documented contract — it would also leave the next-tasks.md entry 7 ("LocalGCP integration in CI") permanently blocked. Alternative (d) requires the LocalGCP Go toolchain inside the Dockerfile and adds compile-time dependencies (Go module cache, build-time network access to Go proxies), inflating the build surface and CI cost for no concrete gain — the release tarball is signed by the upstream maintainer and pinning a specific version is equivalent to source pinning for our purposes. The chosen build-from-tarball approach: (i) uses the only public, anonymously-accessible distribution channel the upstream actually publishes; (ii) keeps the final image lean (~110 MB) via a multi-stage build that discards the downloader stage; (iii) supports multi-arch via BuildKit `TARGETOS`/`TARGETARCH` build args (`linux/amd64` and `linux/arm64` for Apple Silicon); (iv) runs the emulator as root inside the container with `chmod 0777` on `/data/.localgcp` so bind-mount permission collisions with arbitrary host UIDs are eliminated (the earlier attempt at running as a non-root `localgcp` user inside the container failed because bind-mounted host directories carry host UID ownership, blocking the container user from writing `credentials.json`); and (v) pins the upstream release via the `LOCALGCP_VERSION=0.6.0` build arg so a `docker compose ... build` is fully reproducible. The implementation lives at `Dockerfile.localgcp` (~155 lines) and the updated `docker-compose.localgcp.yml` (`services.localgcp.build` block). The change satisfies QA CP13 Issue #1's Option A from the suggested-fix block. | The build pulls the upstream tarball over HTTPS at image-build time, so the first `docker compose ... up -d` requires network access; subsequent runs use the locally cached image layer. Mitigation: pinning `LOCALGCP_VERSION` makes the build reproducible across machines; the layer cache covers normal developer iteration; CI can pre-build and cache via GitHub Actions cache or a self-hosted registry. A second risk: running as root inside the container is appropriate for an ephemeral dev/CI emulator but is not appropriate for production. Mitigation: production must not use LocalGCP at all — this is documented in `onboarding-addendum.md` §9.7 ("LocalGCP must NOT be used in production"), the user-provided LocalGCP setup instructions echo the same constraint, and the compose-file header comment in `docker-compose.localgcp.yml` repeats it. A third risk: if the upstream `slokam-ai/localgcp` release URL pattern changes (e.g., the org renames, the asset naming convention shifts), the Dockerfile must be updated. Mitigation: the affected change is one Dockerfile, isolated and small; the failure mode is loud (the `docker compose ... build` fails with a curl 404, not a silent runtime defect). A fourth risk: tarball checksum validation is not currently enforced (no `--checksum` step in the Dockerfile). Mitigation: the upstream tarball is served over HTTPS from `github.com`, which gates supply-chain risk on the same trust boundary the user-provided setup instructions already accept; a future hardening pass can add `sha256sum --check` against a pinned checksum file — captured as a follow-up in `next-tasks.md`. |
| 17 | Playwright E2E CI workflow shape: introduce a new `.github/workflows/verify_playwright-e2e.yml` (plus the path-ignore companion `verify_playwright-e2e-noop.yml`) that invokes the project's `yarn test:e2e` script (= `NODE_OPTIONS=--no-node-snapshot playwright test`) on a Node 22 / Node 24 matrix against an `ubuntu-latest` runner with a `postgres:14` service container. The workflow pre-starts `yarn start example-backend` (with `BLITZY_E2E_TEST_MODE=true`) and `yarn start example-app` in the background, polls `:7007/healthcheck` and `:3000/` for readiness, sets `PLAYWRIGHT_URL=http://localhost:3000` to override the CI branch of `playwright.config.ts`'s `baseURL` ternary, runs the Playwright suite, and uploads the HTML report (always), `node_modules/.cache/e2e-test-results/` (on failure), `/tmp/backend.log` (on failure), and `/tmp/frontend.log` (on failure) as artifacts. The noop companion handles docs-only and changeset-only PRs so the required-status-check name `Playwright E2E ${{ matrix.node-version }}` is reachable on every PR. | (a) Extend the existing `verify_e2e-linux.yml` workflow to invoke `yarn test:e2e` in addition to `yarn e2e-test run`, sharing the same job and runner. (b) Use Playwright's own `webServer` block (the non-CI branch of `playwright.config.ts`) by unsetting `CI` before invoking `yarn test:e2e`, letting Playwright start the backend and frontend itself. (c) Run the Playwright suite inside the existing `ci.yml` test job, alongside the unit tests on the same matrix and runner. (d) Use a single Node version (e.g., 22 only) for the matrix to reduce runner minutes, accepting reduced cross-version regression coverage. (e) Build the production backend bundle (`yarn build:backend`) and serve the static frontend from port 7007 (leaving `baseURL` at the CI default) instead of running `yarn start example-app` for the dev frontend. | The existing `verify_e2e-linux.yml` exercises the upstream Backstage `packages/e2e-test/` framework — a distinct test runner from `@playwright/test`. Mixing them in the same job would couple two unrelated test surfaces and inflate the failure-triage surface (a failing checkout-snippet test would block the Playwright suite and vice versa). Alternative (b) re-introduces the `webServer` block, but `playwright.config.ts`'s `webServer` branch is intentionally disabled in CI (`process.env.CI ? [] : [...]`) so Playwright does not race the CI-injected `actions/upload-artifact` cleanup; toggling `CI` off would also disable the canonical `forbidOnly: true` and `retries: 2` CI behaviors, which we want to keep. Alternative (c) mixes the heavy E2E run (backend bring-up, frontend bring-up, browser binary install) with the lightweight unit suite, ballooning the unit-test job's wall-clock time and preventing future unit-test parallelism. Alternative (d) violates the cross-version coverage commitment in AAP §0.8.2.1 ("Node.js runtime: 22 (and forward-compatible with 24)") — the unit, lint, and existing E2E workflows all matrix on `[22.x, 24.x]` and the Playwright workflow follows suit for consistency. Alternative (e) requires the `yarn build:backend` artifact to bake the frontend bundle (a non-trivial build that adds ~3-5 minutes to every workflow run) for a frontend that's already produced cheaper by `yarn start example-app`; the dev frontend is functionally equivalent for the UI assertions the Playwright tests make. The chosen approach mirrors the canonical Backstage E2E workflow shape (`verify_e2e-linux.yml`'s harden-runner / checkout / setup-node / yarn-install steps) while adding the four Playwright-specific responsibilities: install browser binaries with `--with-deps`, bring backend and frontend up in the background, poll for readiness with a 5-minute timeout, and surface artifact paths the Playwright reporter and trace recorder produce. Closes QA Checkpoint 13 Issue #2 ("Playwright E2E tests not invoked by any GitHub workflow"). | The new workflow doubles the post-install fan-out on every non-docs PR (one Postgres + backend + frontend bring-up per matrix entry, two matrix entries). Mitigation: the `concurrency` block cancels in-progress runs on PR retriggers so push storms do not multiply runner consumption; the `paths-ignore` block excludes docs-only changes from running the workflow at all; artifact upload is conditional on failure (`if: failure()`) to keep storage volume bounded for green runs. A second risk: the 5-minute readiness polls (60 attempts × 5s) for backend and frontend assume the runner can compile the Backstage workspace in ten minutes total; on under-provisioned runners this could time out. Mitigation: the polls dump the backend and frontend logs on timeout (`tail -n 200 /tmp/backend.log`) so the failure cause is diagnosable from the workflow run page without re-running; if the timeout proves too tight in practice, the loop bound can be raised. A third risk: running the dev frontend in CI exercises a slightly different code path from production-served-by-backend (HMR enabled, no minification); a UI regression that manifests only in production would not be caught here. Mitigation: this is the same trade-off the existing `verify_e2e-linux.yml` accepts (it runs `yarn backstage-cli repo build` but then exercises the API surface, not the production-built frontend); the visual regression baselines committed under `packages/app/e2e-tests/__screenshots__/` continue to catch unintended visual changes against the dev shell. A fourth risk: `BLITZY_E2E_TEST_MODE=true` activates a backend code path (the `blitzy-e2e` auth provider + audit-events endpoint) that does not run in production. Mitigation: the production check is enforced at backend startup by the runtime assertion in `packages/backend/src/authModuleBlitzyE2E.ts` (sign-in attempt returns 503 unless the flag is set), and the flag itself defaults to undefined in all `app-config.*.yaml` files; the test-only nature is documented in onboarding-addendum.md §9.2. |
| 18 | Cross-browser Playwright projects: fan each base project produced by `generateProjects()` (from `@backstage/e2e-test-utils/playwright`) out into three browser-specific projects — `<package>-chromium`, `<package>-firefox`, `<package>-webkit` — using Playwright's bundled browser binaries via `devices['Desktop Chrome'\|'Desktop Firefox'\|'Desktop Safari']` instead of the original `channel: 'chrome'`. The transformation lives entirely in `playwright.config.ts` (a `browsers.flatMap(...)` over the `generateProjects()` output) so the public `generateProjects` signature is unchanged. Visual regression tests (those whose `test()` title begins with `Visual regression:`) are gated to chromium-only via a `test.beforeEach` hook in each test file (`packages/app/e2e-tests/app.test.ts` and `packages/app/e2e-tests/SearchPage.test.ts`) that calls `test.skip(testInfo.title.startsWith('Visual regression:') && browserName !== 'chromium', '...')`. The 10 existing PNG baselines under `packages/app/e2e-tests/__screenshots__/app.test.ts/` and the 4 baselines anchored to `SearchPage.test.ts` remain the single source of visual truth, captured on chromium. Functional tests (welcome page, theme correctness, sidebar/View/Documentation/star/System/Owner removals, top-bar placement, Support email, library border, catalog count, Guest sign-in audit events, BlitzyPermissionPolicy read-only enforcement, landing redirect, search behavior — approximately 38 of the 52 tests) run on all three browsers. The total test count expands from 52 (chromium-only) to 156 (52 × 3 browsers). | (a) Modify the public `generateProjects()` signature in `packages/e2e-test-utils/src/playwright/generateProjects.ts` to accept a `channels?: string[]` array and emit one project per `(package, channel)` pair. (b) Add `{projectName}` to `snapshotPathTemplate` and migrate the existing 10 chromium baselines into a `chromium/` subdirectory, letting firefox and webkit autogenerate fresh baselines on first run. (c) Restrict the entire visual-regression file (`app.test.ts`'s 10 visual-regression cases and `SearchPage.test.ts`'s 4) to a chromium-only project by `testMatch`/`testIgnore` filtering in `playwright.config.ts` instead of a runtime `test.skip` hook. (d) Use Playwright's `channel: 'chrome'`, `channel: 'firefox-stable'`, `channel: 'msedge'` instead of `devices[…]` so each project still launches a system-installed browser. (e) Keep the single chromium-only project shape and document the AAP §0.8.2.5 cross-browser claim as aspirational. | Alternative (a) modifies a public export (`@public` JSDoc tag at line 26 of `generateProjects.ts`); doing so risks downstream consumer breakage and contravenes AAP §0.3.2's "All upstream Backstage plugins not enumerated above" boundary — `@backstage/e2e-test-utils` is a published package and its API surface stability is governed by Backstage's own release process. Alternative (b) inflates the baseline storage surface threefold (30 PNGs instead of 10) and accepts cross-browser pixel drift as the visual contract, but cross-browser pixel rendering is an industry-acknowledged source of flake — font hinting differs between Chrome and Firefox, scrollbar widths differ between WebKit and Chromium, sub-pixel anti-aliasing differs between all three engines — so the autogenerated firefox/webkit baselines would either fail spuriously on subsequent runs or require per-engine `maxDiffPixels`/`threshold` tuning (currently 100 and 0.2) for each browser. Alternative (c) eliminates the runtime check but couples the visual-regression scope to file-level boundaries (every visual-regression test must live in a separate file from functional tests). The current `app.test.ts` mixes both surfaces (functional + visual) deliberately so that the functional and visual assertions for the same screen are colocated for diff readability; splitting them would inflate the file count and reduce review locality. Alternative (d) requires three system-installed browsers in CI (Chrome stable + Firefox stable + Edge), inflating the CI image surface and pinning the test outcome to whatever browser binary the runner happens to provide on a given day — incompatible with the deterministic-CI commitment in AAP §0.8.1.1. The chosen approach uses Playwright's own bundled browsers (downloaded via `npx playwright install --with-deps` in CI), so the browser binary versions are pinned by `@playwright/test`'s release rather than by the runner image; the fan-out is local to `playwright.config.ts`; the visual regression scope is enforced at runtime by a hook that needs zero changes if a new visual-regression test is added (the hook matches by title prefix); and the existing 10 PNG baselines remain authoritative without migration. Closes QA Checkpoint 13 Issue #4 ("Single browser channel ('chrome') in Playwright projects — no cross-browser regression coverage"). | Cross-browser baselines for firefox and webkit are not collected by this PR, so the AAP §0.8.2.5 commitment is technically partial: functional behavior is verified on all three browsers, but visual fidelity is verified only on chromium. Mitigation: visual fidelity on firefox and webkit is captured as a follow-up in [`next-tasks.md`](next-tasks.md) (entry "Cross-browser visual regression baselines"); the per-browser baseline collection is straightforward (`npx playwright test --project=example-app-firefox --update-snapshots` after migrating to a `{projectName}`-aware `snapshotPathTemplate`) but is deferred because the cross-browser pixel-drift tuning (Mitigation §1 above) is non-trivial and would inflate the current PR's scope beyond the four QA findings. A second risk: doubling the CI test count from 52 to 156 (a 3× expansion) inflates the Playwright workflow's wall-clock time on the runner. Mitigation: the `test.skip` gate skips visual regression tests on firefox and webkit at runtime with zero browser interaction (the skip happens before `page.goto`), so the actual incremental runner cost is the 38 functional tests × 2 additional browsers = approximately 76 extra test invocations. With the existing 30-second per-test timeout and Playwright's default parallelism (1 worker per project by default; can be raised), the workflow's wall-clock is bounded by the chromium project, not the firefox/webkit projects. A third risk: the runtime title-prefix check `testInfo.title.startsWith('Visual regression:')` is a brittle convention — a future visual-regression test that does not use this prefix will run on firefox/webkit and fail when baselines do not match. Mitigation: the convention is documented as a hard requirement in the JSDoc comment at the top of `playwright.config.ts` ("Visual regression scope:" section) and in each `test.beforeEach` block's inline comment; the unit-test-of-the-convention is the existing 14 `toMatchSnapshot()` call sites, all of which currently follow the convention (verified via `grep -n "Visual regression:" packages/app/e2e-tests/`). |

### 2.1 Notes on Section 2

- Row 7 (feature flags) is a process decision more than a code decision; it constrains how this PR is shipped and rolled back. The decision is recorded here because a competent engineer could reasonably have preferred phased rollout.
- Row 8 (data-model preservation) is the most likely source of future confusion. The phrase "full removal" in the user's requirement is interpreted as "full removal of the user-facing surfaces", consistent with AAP §0.3.2's explicit out-of-scope boundary around the catalog backend persistence layer.
- Row 9 (per-entity TechDocs) clarifies a scoping ambiguity in the word "Documentation tab". The implicit prerequisite was already called out by the AAP authors in §0.1.1; this row makes the decision auditable.
- Rows 10, 11, 12, and 13 are added in response to QA Checkpoint 5 findings. They document the architectural fixes that close two CRITICAL defects (on-behalf-of token email gap, and the 404 audit-events endpoint) and one MAJOR coverage gap (user-login emission on the blitzy-e2e provider). Each row references the specific QA defect it addresses so reviewers can trace the requirement-to-fix chain end-to-end. Together, these four rows form a single architectural change (the "two-source email architecture") that is conceptually one decision but is documented as four rows because each row independently could have been chosen differently (different cache strategy, different endpoint registration, different resolver shape, different service registration scope).
- Row 10 is the foundational decision; rows 11, 12, 13 are companion decisions that complete the architectural surface. Row 13 is the smallest in code surface (a single `backend.add(...)` line in `index.ts`) but is logically central — without it, the custom user-info service never displaces the default factory and the policy's PRIMARY email source path produces `undefined`.
- Rows 14 and 15 are added in response to QA Checkpoint 10 (security and CVE) findings. Both rows are deviations from the "do nothing" interpretation because the underlying CVEs and the bind-address default were inherited from the upstream Backstage configuration rather than introduced by this refactor. Row 14 disables two unused experimental OIDC features because they trigger CVE-2026-32235 (CVSS 5.9 MEDIUM, OAuth redirect URI allowlist bypass) and CVE-2026-32236 (SSRF in CIMD metadata fetch) at the configuration level. Row 15 changes the `/metrics` bind default from "all interfaces" to `127.0.0.1` so the OpenTelemetry `target_info` disclosure (process owner, host id, command path, runtime version, full endpoint inventory) is unreachable from external network neighbors. Both rows preserve a single environment-variable escape hatch (`PROMETHEUS_BIND_HOST` for row 15) so operators can opt into the previous behavior if their network layer enforces the boundary, but the safer default is now the out-of-the-box experience. Neither change introduces a new test surface because the underlying behavior is configuration-level (verified by a runtime curl from non-local interface returning connection-refused after the bind change).
- Row 16 is added in response to QA Checkpoint 13 Critical Issue #1 (LocalGCP container image not publicly pullable). The row documents the choice to build LocalGCP from the upstream public tarball via a new `Dockerfile.localgcp` rather than referencing the externally hosted but unauthenticated `ghcr.io/slokam-ai/localgcp:v0.6.0` image. The decision closes the R6 (LocalGCP Verification) gap that QA CP13 flagged: prior to this row, AAP §0.4.3's compose-based emulator path was aspirational because the referenced image returned `unauthorized` from anonymous GHCR pulls. After this row, the compose path is unconditionally reachable from every developer machine and every CI runner without GHCR credentials, with the upstream version pinned via the `LOCALGCP_VERSION` build arg for reproducible builds.
- Row 17 is added in response to QA Checkpoint 13 Issue #2 (Playwright E2E tests not invoked by any GitHub workflow). The row documents the choice to introduce a new `verify_playwright-e2e.yml` workflow (with the standard `verify_playwright-e2e-noop.yml` companion) rather than extending an existing workflow or relying on Playwright's own `webServer` block to bring up the backend and frontend. The decision closes the AAP §0.8.1.1 ("GitHub Checks: The final Pull Request MUST pass all GitHub checks") + §0.8.1.2 ("E2E/UI test coverage of every UI/UX Modification and Feature Removal item") gap: prior to this row, the 47 Playwright tests under `packages/app/e2e-tests/` could be invoked locally via `yarn test:e2e` but were never exercised by CI, so PR gates could not enforce that they pass. After this row, every non-docs PR runs the full Playwright suite on a Node 22 / Node 24 matrix against a Postgres 14 service container with `BLITZY_E2E_TEST_MODE=true`.
- Row 18 is added in response to QA Checkpoint 13 Issue #4 (Single browser channel ('chrome') in Playwright projects — no cross-browser regression coverage). The row documents the choice to fan the per-package projects produced by `generateProjects()` out across chromium / firefox / webkit using Playwright's bundled browsers (`devices` presets) instead of modifying the public `generateProjects()` signature. Visual regression tests remain chromium-only via a `test.beforeEach` title-prefix gate so the existing 10 PNG baselines stay authoritative without migration; functional tests run on all three browsers. After this row, AAP §0.8.2.5's cross-browser commitment is verified end-to-end in CI for functional behavior, with visual fidelity captured as a follow-up in [`next-tasks.md`](next-tasks.md). The decision pairs with Row 17 (CI workflow) — Row 17 makes the Playwright suite gate enforceable; Row 18 makes that gate exercise three browsers instead of one.

---

## 3. See also

The following cross-references support traceability between this decision log and the rest of the refactor deliverables. Each link is repository-relative so it resolves correctly in GitHub, GitLab, and any Markdown previewer.

- [`traceability-matrix.md`](traceability-matrix.md) — Maps each decision to the requirement it serves and the files it affects. The matrix is bidirectional: each row's decision number appears in the requirement → implementation column of the corresponding requirement.
- [`architecture-before-after.md`](architecture-before-after.md) — Mermaid diagrams showing the chrome and permission layers before and after. Diagrams visualize the choices made in rows 2 (Frontend Composition), 3 + 4 + 6 (Authorization and Audit), and 1 (Catalog Count).
- [`onboarding-addendum.md`](onboarding-addendum.md) — Customization guidance referenced from rows 5 (Logo customization), 6 (Policy Customization §6), and 8 (Common Pitfalls §8).
- [`next-tasks.md`](next-tasks.md) — Discovered improvements referenced from row 9 (TechDocs replacement candidate) and other rows where future enhancement paths were identified but deferred.
- [`../architecture-decisions/`](../architecture-decisions/) — Upstream Backstage ADRs (different format; this decision log is intentionally distinct, as noted in the header). The upstream ADR format remains the canonical location for Backstage-governance-scope decisions.
- [`../auth/identity-resolver.md`](../auth/identity-resolver.md) — Email extraction documentation referenced from row 6. Documents the augmented `signInResolver` after the refactor.
- [`../auth/github/provider.md`](../auth/github/provider.md) — GitHub provider audit emission documentation referenced from row 6. Documents the `user-login` audit event added by the augmented resolver.
- [`../permissions/writing-a-policy.md`](../permissions/writing-a-policy.md) — Upstream policy authoring guide referenced by row 4's design. The `BlitzyPermissionPolicy` follows the patterns documented there.

### 3.1 Decision Audit Trail

The decision log is itself an audit artifact. Reviewers can cross-check every decision against:

- The AAP sub-section cited in the Rationale column.
- The file(s) touched by the decision, listed in [`traceability-matrix.md`](traceability-matrix.md).
- The test(s) that verify the decision's correctness, also listed in the traceability matrix under the "Verifying Tests" column.
- The Mermaid diagram in [`architecture-before-after.md`](architecture-before-after.md) that visualizes the change introduced by the decision.

Together, these four artifacts form a complete chain: requirement → decision → implementation → test → visualization. Any link missing in the chain is a defect to be repaired before merge.

### 3.2 Executive Presentation Cross-Reference

Per AAP §0.7.1.5 (R5 Executive Presentation), the non-technical leadership summary lives at [`../../blitzy-deck/executive-summary.html`](../../blitzy-deck/executive-summary.html). The executive presentation reproduces the same Mermaid diagrams that visualize the decisions in this log, and its risk-register slide derives its content from the Risks column of Sections 1 and 2 above. Decisions in this log are the source of truth; the presentation is the audience-tailored projection.

### 3.3 Mapping to Agent Action Plan Rules

The decisions in this log collectively address the following AAP rules:

- **R1 (Observability)** — Row 3 (Audit middleware location) is the implementation surface for the audit observability stream enumerated in AAP §0.7.1.1. Rows 10 and 12 (email cache + user-login emission on blitzy-e2e) extend the observability stream to cover internal permission paths and the test-mode sign-in provider, closing the gaps identified in QA Checkpoint 5.
- **R2 (Onboarding & Continued Development)** — Rows 5, 6, 8, and 9 are referenced from [`onboarding-addendum.md`](onboarding-addendum.md) so a new developer can read the WHY behind the customization guidance. Row 10 (email cache architecture) is documented at length in [`onboarding-addendum.md`](onboarding-addendum.md) §6.6 with a debugging walkthrough; row 13 (custom UserInfoService) is referenced from the same section.
- **R3 (Explainability)** — This entire document. The flat-table format and the three sections (decisions, deviations, see also) are mandated by AAP §0.7.1.3.
- **R4 (Visual Architecture Documentation)** — The architectural decisions in rows 2, 3, 4, and 6 are visualized in [`architecture-before-after.md`](architecture-before-after.md). The decision log is the textual companion to those diagrams. Rows 10 and 13 introduce a new substrate (the email cache + custom user-info service) that warrants a future diagram extension; see [`next-tasks.md`](next-tasks.md) for the suggested follow-on diagram work.
- **R5 (Executive Presentation)** — See §3.2 above.
- **R6 (LocalGCP Verification)** — Row 16 (LocalGCP Docker Compose build-from-tarball) is the implementation surface for the compose-based emulator path mandated by AAP §0.4.3 and §0.7.1.6. The row closes the gap that QA Checkpoint 13 Critical Issue #1 identified, where the previously referenced `ghcr.io/slokam-ai/localgcp` image was not anonymously pullable and the compose-based developer/CI workflow was therefore non-functional. The refactor itself does not introduce new GCP integrations, but the emulator-provisioning contract that R6 requires is now actually deliverable through the documented path.
- **R7 (LLM Request Validation Limit)** — Not applicable; the refactor does not introduce LLM API calls.

Row 17 (Playwright E2E CI workflow) is not directly mapped to a single AAP rule; it operationalizes two AAP process directives — §0.8.1.1 ("GitHub Checks: The final Pull Request MUST pass all GitHub checks") and §0.8.1.2 ("E2E/UI test coverage of every UI/UX Modification and Feature Removal item"). Without the workflow, the second directive's test files exist but cannot be enforced as PR gates; the workflow makes those gates real.

Row 18 (Cross-browser Playwright projects) operationalizes AAP §0.8.2.5 ("Browser compatibility: Chromium, Firefox, WebKit (as configured in `playwright.config.ts`). The Playwright E2E suite runs against all three projects; the chrome refactor is verified on all three."). Without the cross-browser fan-out, the AAP's cross-browser commitment is documented but not exercised. The decision pairs with Row 17: Row 17 makes the workflow gate enforceable, Row 18 makes the gate exercise three browsers. Visual fidelity on firefox / webkit remains a follow-up tracked in [`next-tasks.md`](next-tasks.md) because the per-browser pixel-drift tuning is non-trivial and out of scope for this PR.
